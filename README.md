# awsLambdaFunction
Repositório destinado a um projeto de serveless function, utilizando AWS Lambda e NodeJS (nativo) do serviço. A função acessa o bucket no S3 onde estão armazenados os arquivos, verifica o tamanho e, dependendo do tamanho deste arquivo, deleta-o ou mantém ativo.
